# dsh-visual-aid bundle layer. Applied after dsh-base. The service is dormant
# until the `visual-aid` settings section enables it; uninstall removes the row.
- insert:
    - id: visual-aid
      name: '@deepseek-ai/dsh-visual-aid'
    - id: ui-visual-aid
      name: '@deepseek-ai/dsh-client-ui-visual-aid'
